{
  "name": "kdd-engine-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --build",
    "bench": "bun run bench/compare.ts"
  },
  "devDependencies": {
    "@kdd/core": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
